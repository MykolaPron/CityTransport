import {TSelectedRouteColor} from "../types";

export const CSelectedRouteColorList: TSelectedRouteColor[] = ["primary", "success", "warning", "info", "danger"];
export const CDefaultRouteDirection = 'direct'
